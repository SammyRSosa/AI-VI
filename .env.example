# ────────────────────────────────────────────────────
# .env.example — copia este archivo a .env y rellena
#                tus API keys reales.
# ────────────────────────────────────────────────────

# ── Proveedor LLM (elige uno) ──────────────────────
# Opción 1: Google Gemini (gratuito)
GOOGLE_API_KEY=tu_google_api_key_aqui

# Opción 2: Groq (gratuito con rate limit)
GROQ_API_KEY=tu_groq_api_key_aqui

# Opción 3: xAI (Grok)
XAI_API_KEY=tu_xai_api_key_aqui

# Opción 4: OpenAI
OPENAI_API_KEY=tu_openai_api_key_aqui

# Opción 4: Anthropic
ANTHROPIC_API_KEY=tu_anthropic_api_key_aqui

# ── Configuración del LLM ─────────────────────────
# Proveedor activo: "gemini", "groq", "openai", "anthropic"
LLM_PROVIDER=gemini

# Modelo específico a usar:
#   - Gemini: gemini/gemini-3.1-flash-lite
#   - Groq: groq/llama-3.3-70b-versatile (o groq/llama3-8b-8192)
#   - Ollama (Local): ollama/llama3
LLM_MODEL=gemini/gemini-3.1-flash-lite

# Temperatura (0.0 = determinístico, recomendado para evaluación)
LLM_TEMPERATURE=0.0

# Máximo de tokens en la respuesta del LLM (1024 recomendado para evitar respuestas JSON truncadas)
LLM_MAX_TOKENS=1024

# ── Caché ─────────────────────────────────────────
# Directorio donde se guarda el caché en disco
LLM_CACHE_DIR=data/llm_cache

# ── Experimentos ──────────────────────────────────
# Semilla aleatoria para reproducibilidad
RANDOM_SEED=42
